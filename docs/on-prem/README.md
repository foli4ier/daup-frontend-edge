# Seed setup (placeholder pack)

Hub download of this pack: `/on-prem/seed-setup.zip` on app.daup.co.za.

This is a **placeholder**. The live on-prem installer is not in this repo. Source files live in `docs/on-prem/seed-setup/`. The zip Hub serves is `daup-frontend-edge/public/on-prem/seed-setup.zip`.

Door copy: **Download seed setup.** Artifact type: `.zip` (scripts + README, not a Windows `.exe`).
