{
  "name": "daup-frontend-edge-root",
  "private": true,
  "description": "Repo root delegator — the Worker app lives in daup-frontend-edge/",
  "scripts": {
    "dev": "npm run dev --prefix daup-frontend-edge",
    "build": "npm ci --prefix daup-frontend-edge && npm run build --prefix daup-frontend-edge",
    "deploy": "npm run deploy --prefix daup-frontend-edge",
    "preview": "npm run preview --prefix daup-frontend-edge"
  }
}
